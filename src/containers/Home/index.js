import Map from "./Map";

const Home = () => {
  return (
    <div
      style={{
        height: "100vh",
        width: "100%",
      }}
    >
      <Map />
    </div>
  );
};

export default Home;
